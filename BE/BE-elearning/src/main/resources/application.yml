spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ttcn?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: tailieu21001@gmail.com
    password: ukhjqqawxxcbodae
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

  logging:
    level:
      org.springframework.security: DEBUG
